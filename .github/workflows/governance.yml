name: Governance Self-Test

on:
  pull_request:

jobs:
  governance:
    name: Governance Engine
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Step
        run: echo "Governance logic has been successfully migrated to ci.yml. This dummy job satisfies legacy branch protection rules."

  enforce:
    name: Enforce Governance
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Step
        run: echo "Passed."
