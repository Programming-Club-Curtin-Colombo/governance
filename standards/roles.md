1. Roles
student
external
maintainer
2. What each role can do (high level)

Example:

student → contribute via PR
external → restricted + approval required
maintainer → bypass + merge authority

Keep it conceptual, not technical.